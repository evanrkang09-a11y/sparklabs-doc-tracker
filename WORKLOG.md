# 작업 기록 — 서류 취합 트래커 + 서류 실사 체크리스트

**Work log — Document Collection Tracker & Due Diligence Checklist**

작성자 / Author: Evan Kang
날짜 / Date: 2026-08-13 (최초 작성 2026-08-12)
배포 링크 / Live app: https://sparklabs-doc-tracker.vercel.app

> 다음 기수 인턴을 위한 참고 자료입니다. 프롬프트 원문과 막혔던 지점을 그대로 남겼습니다.
> Written for the next intern. The prompts and the dead ends are recorded as they actually happened.

---

## 1. 무엇을 만들었나 / What this is

네 개의 화면입니다. (1주차에는 ②③만 있었고, ①④는 2주차에 추가했습니다.)

**⚠️ 중간에 바뀐 전제 — 누가 올리는가:** 처음에는 기업이 링크를 받아 직접 올리는 구조로 만들었습니다. 그런데 멘토님이 다시 정리해 주셨습니다 — **기업이 이메일로 서류를 보내오고, 그것을 스파크랩스 직원이 올립니다.** 그래서 지금은 **사이트 전체가 로그인 뒤에** 있고, 외부인이 열 수 있는 화면은 하나도 없습니다. 이 한 문장이 설계를 크게 바꿨습니다 (아래 §6 참고).

**① 홈 / 진행 현황** — `/`
기업 목록 + 각 기업의 진행률. 왼쪽에 사이드바(배치별 필터·보관함), 위에 전체 현황 요약이 있습니다. 화면에서 **기업을 직접 추가**할 수 있고(이름 / 국내·해외 / 딜 유형), 배치를 만들어 기업을 배치에 넣을 수 있습니다. 끝난 기업은 **보관(archive)**, 정말 필요하면 **완전 삭제**도 됩니다. 사이드바에 **단계 필터**가 있어서 현재 어느 단계에 있는 기업만 골라볼 수 있습니다 — 서류 수집 중 / 실사 중 / 계약 준비, 각 단계의 기업 수도 함께 표시됩니다.

**② 서류 취합 트래커** — `/deal/<기업>`
투자 전 제출 서류 목록. 파일을 올리면 파일명으로 어떤 서류인지 자동 판별해서 체크가 됩니다. 잘못 올린 파일은 지울 수 있고, 파일명으로 판별하지 못한 파일은 **AI가 어떤 서류인지 추정**해 줍니다. 기업이 **ZIP으로 한꺼번에 보내온 경우 압축을 풀어** 개별 서류로 인식합니다.

**③ 서류 실사 체크리스트** — `/diligence/<기업>`
제출된 서류들을 서로 대조하는 실사 항목 13개. 각 항목마다 체크박스와 메모가 있고 자동 저장됩니다. 항목별로 **여러 명이 같이 쓰는 코멘트** 칸이 있습니다.
그리고 **AI가 실제 서류를 읽습니다.** 파일명이 아니라 PDF 내용을 읽어서, 그 항목의 기준이 충족됐는지 판단하고 근거를 설명하고 체크 여부를 제안합니다. 표준 13개 항목 밖에서 발견한 것은 별도 칸에 따로 보여줍니다.

**④ 투자계약서 작성** — `/agreement/<기업>`
멘토님이 주신 계약서 양식(Word)을 **사이트에서 바로 채웁니다.** 원본에서 노란색으로 칠해져 있던 자리 85곳이 오른쪽 입력칸 57개와 연결되어 있어서, 타이핑하면 왼쪽 계약서 미리보기에 바로 들어갑니다. 다 채우면 **.docx / .pdf로 내려받습니다.** **AI 자동 입력** 버튼을 누르면 업로드된 서류를 읽어 빈 항목에 값을 제안합니다 — 항목 옆에 '적용' 칩이 뜨고, '전체 적용' 버튼으로 한 번에 채울 수 있습니다. 제안은 회색 흐린 글씨로만 표시되고 사람이 수락하기 전에는 실제로 입력되지 않습니다. 저장 버튼과 PDF 저장 버튼은 색상이 다르게(초록색 채우기 vs. 테두리만) 구분됩니다.

### 서류 목록 / The checklist

- 국내 기업: 필수 18건 + 해당 시 3건
- 해외 기업: 필수 10건 + 해당 시 1건

두 리스트는 번역 관계가 아니라 **내용이 실제로 다릅니다.** (예: 해외는 Cap Table이 필수, 국내는 4대보험 가입자명부가 필수)

### 실사 항목 / Due diligence items

멘토님의 「#3. 서류 실사」 문서 13개 항목을 두 그룹으로 나눴습니다.

- **서류 검증 (1~7번)** — 사업목적 일치, 발행주식수 일치, 정관 종류주식·신주인수권, 옵션풀, 등기 순서, 차입금·가수금, 주주 구성, 지식재산권, 최종 대조
- **후속 절차 (8~13번)** — 서면 communication, 예비실사 고지, 계약서 작성, 계약 체결, 재무팀 공유, 투자심사보고서

### 기술 스택 / Stack

| | |
|---|---|
| Framework | Next.js 16.3 (App Router) + React 19.2 |
| Language | TypeScript |
| Styling | Tailwind CSS v4 |
| 파일 저장 | Vercel Blob (private) |
| AI | Google Gemini API (직접 연결) — `gemini-2.5-flash-lite` |
| 로그인 | Auth.js (next-auth v5) + 구글 로그인, `@sparklabs.co.kr`만 허용 |
| 계약서 | Word 파일(.docx)을 직접 열어 채움 — `fflate` |
| 배포 | Vercel |

### 파일 구조 / Where the code lives

| 파일 | 역할 |
|---|---|
| `lib/documents.ts` | 필수 서류 목록 + 파일명 매칭. **여기만 고치면 서류 목록이 바뀝니다.** |
| `lib/diligence.ts` | 실사 항목 13개 정의 |
| `lib/deals.ts` | 기업·배치 타입 정의 + 처음 들어 있는 샘플 기업 |
| `lib/deals-store.ts` | 기업 추가·수정·보관·삭제 (화면에서 추가한 기업이 여기 저장됩니다) |
| `lib/deal-status.ts` | 어떤 서류가 도착했는지 계산 (모든 화면이 공유) |
| `lib/storage.ts` | 업로드된 파일 목록 조회·삭제·읽기 |
| `lib/unzip.ts` | ZIP 압축 풀기 (한글 파일명 처리 포함) |
| `lib/diligence-store.ts` | 실사 체크·메모 저장 |
| `lib/comments-store.ts` | 실사 항목별 코멘트 (한 코멘트 = 한 파일) |
| `lib/classify.ts` | AI 파일명 추정 |
| `lib/analysis.ts` | **AI가 서류 내용을 읽고 실사 항목을 판단** |
| `lib/openrouter.ts` | AI 호출 창구 하나 — Google Gemini에 직접 연결; 모델 교체는 여기 |
| `lib/agreement-suggest.ts` | AI가 업로드 서류를 읽어 계약서 입력칸 제안 값을 반환 |
| `auth.ts` / `proxy.ts` | 로그인 + 로그인 안 한 사람 차단 |
| `lib/agreement-fields.ts` | 계약서 입력칸 57개 ↔ 양식 자리 85곳 연결표 |
| `lib/agreement-docx.ts` | Word 파일에 값을 넣어 완성본 만들기 |
| `lib/agreement-store.ts` | 계약서 작성 내용 저장 (기업별, 전 직원 공유) |
| `templates/investment-agreement.docx` | 채울 자리가 표시된 계약서 양식 |
| `app/deal/[dealId]/` | 서류 업로드 화면 |
| `app/diligence/[dealId]/` | 실사 화면 |
| `app/agreement/[dealId]/` | 계약서 작성 화면 |
| `app/api/deals/[dealId]/agreement/suggest/` | AI 자동 입력 제안 POST 엔드포인트 |
| `app/api/upload/` | 업로드 토큰 발급 |
| `scripts/bench-models.mjs` | AI 모델 비교 테스트 (아래 §5 참고) |
| `scripts/prepare-template.py` | 계약서 양식에서 노란 칸을 찾아 채울 자리로 바꾸는 도구 (아래 §10) |

---

## 2. 중간에 통째로 갈아엎은 것 / The rewrite

**1차 버전은 방향이 틀렸습니다.** 처음에는 멘토님의 구글 드라이브 샘플 폴더를 앱이 읽어서, 그 폴더 상태를 화면에 보여주는 구조였습니다. 데모로는 그럴듯했지만 실무에서 쓸 수 없는 물건이었습니다.

멘토님 피드백 세 가지:

1. **기업이 직접 올릴 수 있어야 한다.** 우리가 폴더를 들여다보는 게 아니라, 기업별로 링크를 주고 기업이 업로드하는 구조여야 합니다.
2. **서류 목록이 틀렸다.** 9건이 아니라 국내 18건 / 해외 10건입니다.
3. **파일 번호의 6번, 9번이 비어 있던 건 아무 의미가 없다.** 샘플 배치를 만들면서 몇 개를 뺀 것뿐입니다.

3번이 특히 뼈아팠습니다. 저는 **1차 작업 기록에 "6번과 9번이 무슨 서류인지 확인 필요"라고 적어놨었습니다.** 데이터에 있는 패턴이 전부 의미 있는 건 아닙니다. 추측을 기록으로 남기기 전에 물어봤어야 했습니다.

### ⚠️ 과업지시서와 다르게 만든 부분 (멘토님 지시)

**과업지시서 원문 (3.미니과제 탭, 핵심 기능 2번):**

> **구글 드라이브 업로드 인식** — 멘토가 미리 만들어둔 드라이브 폴더에 서류 파일을 올리면, 웹 앱이 '어떤 서류가 올라왔는지' 파일명으로 판별

**실제로 만든 것:** 기업별 링크를 통한 **직접 업로드**. 앱이 드라이브 폴더를 들여다보는 방식이 아닙니다.

**이유:** 멘토님이 구두로 다르게 지시하셨습니다. "파일을 올리는 사람이 웹사이트에 직접 올리도록 유도하는 형태여야 한다, 지금처럼 우리가 특정 폴더를 보고 결과를 보여주는 방식이 아니라 **사용자별(기업별) 케이스**로 동작해야 한다."

즉 **문서에는 드라이브 인식이라고 적혀 있지만, 멘토님은 직접 업로드를 요구하셨고 그쪽을 따랐습니다.** 실무에서는 우리가 기업 폴더를 들여다보는 게 아니라 기업이 자기 서류를 제출하는 것이므로, 구두 지시 쪽이 실제 업무 흐름에 맞습니다.

**드라이브 연동 코드는 지우지 않고 꺼두었습니다** (`lib/drive.ts`, `readsSampleDriveFolder: false`). 원래 요구사항대로 되돌려야 할 일이 생기면 스위치만 켜면 됩니다.

**교훈:** 문서와 사람 말이 다르면 사람 말이 최신입니다. 다만 **왜 다르게 만들었는지는 반드시 기록**해 두어야, 나중에 "요구사항 2번은 왜 안 했냐"는 질문에 답할 수 있습니다.

**갈아엎으면서 얻은 것:** 드라이브 연동 코드(`lib/drive.ts`)는 지우지 않고 스위치만 꺼뒀습니다(`readsSampleDriveFolder: false`). 나중에 필요하면 다시 켜면 됩니다. git에도 남아 있습니다.

---

## 3. 어떻게 만들었나 / How it was built

전부 Claude Code(터미널에서 실행되는 AI 코딩 도구)와 대화하면서 만들었습니다. 저는 코딩 경험이 없는 상태로 시작했습니다.

**가장 중요한 교훈: 한 번에 완성되는 큰 프롬프트는 없었습니다.** 짧은 요청을 계속 주고받는 방식이 훨씬 효과적이었습니다.

### 실제로 사용한 프롬프트 / The prompts I actually used

과제 파악:

```
can you find the original korean file for me and just translate it
```

→ 영어 버전 파일도 있었지만 불완전해서, 한글 원본을 직접 읽게 하는 편이 정확했습니다.

방향 수정 (가장 중요했던 프롬프트):

```
ok so basically, what we have right now is not good enough. i talked with my
mentor. he said that firstly, we want it to be something where the person who
is uploading these files can be prompted to upload their files onto the
website, so that it is user by user case, not like right now where we just
went and showed our results to our specific file. also, our list of documents
needed was incorrect. we needed much more, ill attach the list.
```

→ **멘토님 말씀을 정리하지 말고 그대로 옮기는 게 나았습니다.** 요약하면 뉘앙스가 날아갑니다.

검증:

```
hey so where did you get the info regarding zest, and why is it checking off
which documents we apparently have without the user having uploaded a file
```

→ 화면이 이상하면 바로 물어보세요. 실제로 버그였습니다. (아래 ②번)

모델 선택:

```
we have an openrouter api that was given to me to find the most cost efficient
and best fitting api for our tasks. so find the best one please.
```

→ "제일 좋은 거 골라줘"가 아니라 **기준(비용·적합성)을 같이 주면** 추측 대신 실제 측정을 하게 됩니다.

이해가 안 될 때:

```
im kinda lost, what did you just do. explain it to me like im 5
```

**2주차에 가장 잘 통한 프롬프트 세 개 —**

방법을 모를 때는 **"만들어줘"가 아니라 "가능한가요"라고 물었습니다:**

```
my mentor wants a login page that only accepts sparkslabs employees. how would
we do that and is that possible without their explcit passwords to their
sparkslabs emails?
```

→ 만들기 전에 **선택지를 먼저 받았습니다.** 비밀번호를 받지 않아도 된다는 걸 여기서 알았습니다.

전제가 바뀌었을 때는 **그 한 문장만 정확히** 전했습니다:

```
companies wont be the ones uplaoding the files, it will be sparklabs employees
that upload them once the companies send it to them externally
```

→ 이 한 줄이 사이트 전체를 로그인 뒤로 옮겼습니다. **"누가 쓰는가"는 기능보다 먼저 정해야 합니다.**

안 되는 걸 봤을 때:

```
theres an issue with the ai analysis, it says ai analysis failed on every
single check. we need this fixed right now
```

→ **"전부"와 "매번"은 중요한 정보입니다.** 하나만 실패하면 그 서류 문제지만, 전부 실패하면 공통 원인이 있습니다.

---

## 4. 막혔던 지점과 해결 / What went wrong, and how it got fixed

평가 기준이 "결과보다 과정"이라고 되어 있어서, 실제로 막혔던 부분을 전부 남깁니다.

### ① 서류 이름이 두 가지였음 — 가장 중요한 버그

드라이브 폴더의 파일은 `등기사항전부증명서`인데, 용어집에는 `법인 등기부등본`으로 적혀 있었습니다. 같은 서류의 다른 이름입니다.

처음 만든 매칭 로직은 `등기부등본`만 찾고 있어서, **파일이 바로 거기 있는데도 "미비"로 표시**됐을 상황이었습니다.

**해결:** 서류 하나에 여러 개의 이름(별칭)을 등록할 수 있게 만들었습니다.

```ts
keywords: ["등기사항전부증명서", "등기사항증명서", "등기부등본", "등기부", ...]
```

**교훈:** 실제 파일을 먼저 확인하지 않고 용어집만 보고 만들었으면 데모에서 틀렸을 겁니다. 코드를 짜기 전에 진짜 데이터를 먼저 보세요.

### ② 아무것도 안 올렸는데 체크가 되어 있었음

새 업로드 화면을 열었더니 **파일을 하나도 안 올렸는데 서류 몇 개가 이미 체크**되어 있었습니다. 멘토님 샘플 드라이브 폴더를 아직 읽고 있었기 때문입니다.

기업 입장에서는 "내가 안 냈는데 냈다고 나오는" 화면입니다. 스위치를 꺼서 해결했습니다.

**교훈:** 화면에 이상한 게 보이면 그냥 넘어가지 마세요. 물어본 덕분에 잡았습니다.

### ③ 4.5MB보다 큰 파일

IR덱은 보통 15~20MB입니다. (제스트 IR덱은 17MB) 그런데 서버를 거쳐 업로드하면 4.5MB에서 막힙니다.

**해결:** 브라우저가 서버를 거치지 않고 저장소에 직접 올리는 방식(client upload)으로 바꿨습니다. 서버는 "이 경로에 올려도 된다"는 토큰만 발급합니다. 한도는 50MB로 잡았습니다.

### ④ 환경변수가 저장되지 않았음

Vercel 대시보드에서 환경변수를 입력했는데 저장이 안 됐습니다. 화면상으로는 알 수 없었고, 배포된 앱이 계속 샘플 데이터를 보여줬습니다.

```
npx vercel env ls
```

**교훈:** 환경변수는 **저장한 이후에 만든 배포부터** 적용됩니다. 저장했으면 반드시 다시 배포해야 합니다.

### ⑤ 눈에 안 보이는 문자 때문에 인증 실패

환경변수 값을 복사해서 붙여넣었더니 맨 앞에 눈에 보이지 않는 문자(BOM)가 딸려 들어갔습니다. 화면상으로는 완전히 똑같아 보이는데 인증만 실패합니다.

**해결:** 값을 읽을 때 앞뒤 공백을 잘라내도록(`.trim()`) 했습니다.

### ⑥ 환경변수 이름에 띄어쓰기

OpenRouter 키를 `.env.local`에 넣을 때 이름을 `OPENROUTER API KEY`로 적었습니다. **환경변수 이름에는 띄어쓰기가 들어갈 수 없습니다.** `OPENROUTER_API_KEY`로 고쳐야 합니다.

에러 메시지가 "키가 없다"고만 나오기 때문에, 파일에 분명히 키가 있는데도 원인을 찾기 어렵습니다.

**교훈:** 환경변수 이름은 **대문자와 밑줄(`_`)만** 씁니다.

### ⑦ 윈도우 PowerShell에서 npx 실행 불가

```
npx : File ...\npx.ps1 cannot be loaded because running scripts is disabled
```

**해결:** `npx` 대신 `npx.cmd`를 쓰거나, 아래 명령으로 한 번만 설정하면 됩니다.

```powershell
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
```

### ⑧ `!`는 PowerShell 명령이 아님

Claude Code 입력창에서 `! notepad .env.local`처럼 앞에 `!`를 붙이면 "이건 AI 말고 터미널이 바로 실행해"라는 뜻입니다. 그런데 이걸 **PowerShell 창에 그대로 치면** 에러가 납니다. PowerShell에서 `!`는 '아니다(not)'라는 뜻이기 때문입니다.

| 어디에 치는가 | 어떻게 치는가 |
|---|---|
| PowerShell (`PS C:\...>`) | `notepad .env.local` |
| Claude Code 입력창 | `! notepad .env.local` |

### ⑨ 테스트했더니 한글이 `???`로 나옴 — 앱 문제가 아니었음

실사 메모에 한글을 저장하고 다시 읽었더니 `???`가 나왔습니다. 앱 버그처럼 보였지만, PowerShell이 요청을 보낼 때 한글을 깨뜨린 것이었습니다. UTF-8로 명시해서 다시 보내니 정상이었습니다.

**교훈:** 테스트가 실패하면 **테스트 도구부터 의심하세요.** 앱을 고치기 전에 확인해야 합니다.

### ⑩ 스파크랩스 이메일로 로그인이 안 됐음 — 또 눈에 안 보이는 문자

로그인을 붙였는데 멘토님이 회사 계정으로 들어가려 하자 **`The OAuth client was not found`** 가 떴습니다.

구글에 등록된 클라이언트 ID와 Vercel에 넣은 값을 **한 글자씩 대조했더니 완전히 같았습니다.** 그래서 브라우저가 실제로 구글에 보내는 주소를 열어봤더니, `client_id=` 뒤에 **눈에 보이지 않는 문자(BOM)가 한 개** 붙어 있었습니다. 구글 입장에서는 존재하지 않는 클라이언트입니다.

원인은 제 쪽이었습니다. PowerShell로 값을 Vercel에 넣을 때 PowerShell이 앞에 BOM을 붙였습니다.

**해결 두 가지 —**

1. 값을 넣을 때: `$OutputEncoding = New-Object System.Text.UTF8Encoding($false)`
2. 값을 읽을 때: 환경변수를 읽는 코드(`lib/env.ts`)에서 BOM을 잘라냅니다. 사람이 조심하는 것보다 코드가 막는 게 낫습니다.

**이 프로젝트에서 BOM 때문에 막힌 게 세 번째입니다** (⑤ 드라이브 인증, 이번 로그인, 그리고 제가 BOM 처리 코드를 쓰다가 그 코드 안에 BOM을 넣은 것). **"똑같아 보이는데 안 된다"면 눈에 안 보이는 문자를 의심하세요.**

### ⑪ 제가 직접 만든 보안 구멍 — 남의 코멘트를 지울 수 있었음

실사 항목에 코멘트를 달고 지우는 기능을 만들었습니다. 지울 때 "누가 지우려는지"를 **브라우저가 보내온 값으로 확인**했습니다.

그러면 브라우저에서 그 값만 바꿔 보내면 **동료가 쓴 코멘트를 지울 수 있습니다.** 자기 것만 지우게 만들었다고 생각했는데, 실제로는 아무 것도 막고 있지 않았습니다.

**해결:** 서버가 **저장된 코멘트를 다시 읽어서** 그 작성자와 로그인한 사람을 비교합니다.

**교훈:** **브라우저가 보내는 값은 전부 거짓일 수 있습니다.** "누구인가"는 서버가 가진 로그인 정보로만 판단해야 합니다.

### ⑫ ZIP을 올렸는데 "안 됐음"

기업이 서류를 ZIP 하나로 묶어 보내는 경우가 많은데, 올려보니 아무 일도 일어나지 않았습니다.

확인해 보니 **업로드는 정상이었습니다** (7.2MB, 저장소에서 확인). 압축을 풀어주는 기능이 애초에 없었을 뿐입니다. "안 된다"의 원인이 실패가 아니라 **미구현**이었습니다.

만들면서 나온 문제: **한글 파일명이 깨졌습니다.** 한국 윈도우에서 만든 ZIP은 파일명을 UTF-8이 아니라 CP949로 적습니다. ZIP 안에는 "이 이름이 UTF-8인지" 알려주는 표시가 있어서, 그 표시가 없으면 CP949로 읽도록 했습니다.

### ⑬ AI 분석이 전부 실패 — 확장자가 없는 파일

실사 항목 13개 전부 "AI 분석 실패"가 떴습니다. 실제 서류로 로컬에서 재현해 보니 진짜 원인이 나왔습니다.

```
Unsupported MIME type: application/octet-stream
```

문제의 파일 이름이 `..._등기부등본.pdf의 사본` 이었습니다. **`.pdf`로 끝나지 않으니** 업로드할 때 "종류를 모르는 파일"로 저장됐고, 구글은 종류를 모르는 파일을 거부합니다.

**해결:** 확장자를 믿지 않고 **파일 내용의 첫 몇 바이트를 봅니다.** PDF는 `%PDF`로 시작합니다. 파일명이 어떻든 내용이 PDF면 PDF로 처리합니다.

**덤으로 고친 것:** 화면에 뜬 `provider returned error`는 아무 정보가 없었습니다. 진짜 메시지는 응답 안쪽에 `error.metadata.raw`로 숨어 있었습니다. 이제 그걸 꺼내서 보여주므로, 다음 사람은 20분 헤매지 않고 바로 원인을 봅니다.

**교훈:** **"실패했다"는 화면 문구를 믿지 말고 진짜 에러 메시지를 찾으세요.** 그리고 사용자에게 보여줄 때는 진짜 메시지를 보여주세요.

### ⑭ 파일 이름의 대괄호가 문제였음 — `[dealId]`

파일 여러 개를 한 번에 고치려고 PowerShell의 `Resolve-Path`를 썼는데, **대괄호가 든 폴더만 조용히 건너뛰었습니다.** PowerShell은 `[dealId]`를 "d, e, a, l 중 한 글자"라는 뜻의 패턴으로 읽습니다. Next.js는 폴더 이름에 대괄호를 쓰기 때문에 프로젝트 대부분이 해당됩니다.

**해결:** 경로를 패턴으로 해석하지 않는 방식(`Join-Path` + 리터럴 경로)으로 바꿨습니다. 소스 코드 수정은 아예 편집 도구로만 합니다.

### ⑮ 빌드가 60초 제한에 걸려 실패 — 코드 문제가 아니었음

배포 전 빌드에서 `/_not-found`, `/favicon.ico` 같은 **아무것도 안 하는 페이지들이** 60초를 넘겨 실패했습니다.

내가 방금 쓴 코드 탓이라고 생각하기 쉬운데, **실패한 페이지들이 새 코드와 아무 상관이 없었습니다.** 노트북이 다른 일로 바빴을 뿐입니다. 캐시(`.next`)를 비우고 다시 돌리니 그대로 통과했습니다.

**교훈:** 실패한 것들의 **공통점**을 보세요. "내가 건드린 곳"이 아니라 "다 같이 느려졌다"면 코드가 아니라 환경입니다.

---

## 5. AI 모델은 어떻게 골랐나 / How the AI model was chosen

회사에서 OpenRouter API 키를 받았습니다. **OpenRouter는 중개소입니다** — 키 하나로 구글·OpenAI·Anthropic 등 400개 이상의 모델을 다 쓸 수 있습니다. 회사별로 따로 가입하고 따로 결제할 필요가 없습니다.

과제는 "가장 저렴하면서 우리 일에 잘 맞는 모델 찾기"였습니다. 감으로 고르지 않고 **직접 테스트를 만들어서 비교했습니다** (`scripts/bench-models.mjs`).

테스트 문제는 **키워드 매칭이 실제로 실패하는 파일명 6개**입니다. 예: `제스트_등기_최신본_v3.pdf`(등기부등본), `주주_명단_최종.xlsx`(주주명부). 마지막 한 개는 `IMG_4821.jpg` — **정답이 "모르겠다"인 문제**입니다. 이걸 자신 있게 찍는 모델은 오히려 위험합니다.

결과 (2026-08-12 기준, 실제 토큰 사용량으로 계산):

| 모델 | 정답 | 소요시간 | 1회 비용 | $1로 몇 번 |
|---|---|---|---|---|
| **google/gemini-2.5-flash-lite** | 6/6 | 2.9초 | **$0.000233** | **4,301회** |
| google/gemini-3.1-flash-lite | 6/6 | 2.1초 | $0.000683 | 1,464회 |
| openai/gpt-5-mini | 6/6 | 9.9초 | $0.002321 | 431회 |
| anthropic/claude-haiku-4.5 | 6/6 | 6.7초 | $0.003187 | 314회 |

**넷 다 6/6이었습니다.** 이 정도 난이도에서는 성능 차이가 안 납니다. 그래서 **성능이 같으면 가격으로 결정**했습니다. gemini-2.5-flash-lite는 claude-haiku-4.5보다 **13배 저렴**하면서 답은 똑같았습니다.

솔직한 한계:

- 문제가 6개뿐이라 모델을 강하게 구분하지는 못합니다.
- gemini-2.5-flash-lite는 맞힌 문제에 전부 확신도 `1.0`을 줬습니다. 확신도 눈금이 거친 편입니다. 다만 모르는 문제(`IMG_4821.jpg`)에는 `0.1`을 줬으므로, 중요한 구분은 됩니다.
- 더 정교한 확신도가 필요하면 gemini-3.1-flash-lite가 나았습니다. 3배 비싸지만 여전히 1회 0.07센트입니다.

**모델 교체는 코드 수정 없이 환경변수 한 줄입니다:** `GEMINI_MODEL=...`. 바꾸기 전에 벤치마크를 다시 돌리세요.

> 참고: 과업지시서에는 "Claude API 연동"이라고 되어 있습니다. OpenRouter를 쓸 때는 `OPENROUTER_MODEL=anthropic/claude-haiku-4.5`로 바꾸면 Claude 연동이 됐습니다. 같은 답을 13배 비싸게 받는 것이라 기본값은 Gemini로 뒀습니다.

### 벤치마크 이후: 중개소를 없앴습니다

가장 적합한 모델을 확인한 뒤에, **OpenRouter를 구글 Gemini API 직접 호출로 교체**했습니다. OpenRouter는 여러 모델을 탐색할 때 편리하지만, 실 서비스에서는 매 호출에 수수료가 붙고, 네트워크를 한 단계 더 거치고, 서드파티에 의존하게 됩니다. 직접 연결하면 비용이 줄고 움직이는 부품도 하나 줄어듭니다.

변경은 `lib/openrouter.ts` 내부에서만 일어났습니다(파일 이름을 유지해서 다른 파일은 건드리지 않아도 됐습니다). 환경변수가 `OPENROUTER_API_KEY`에서 `GOOGLE_AI_API_KEY`로 바뀌었고, 인증 방식이 `Bearer` 헤더에서 `?key=` 쿼리 파라미터로 바뀌었으며, 요청·응답 형식을 OpenRouter 형식에서 Gemini 형식으로 변환하는 코드가 추가됐습니다. AI를 호출하는 다른 네 파일은 아무것도 바꾸지 않았습니다.

---

## 6. 설계에서 신경 쓴 점 / Design decisions worth keeping

**기업용 화면과 내부용 화면이 분리되어 있습니다.** 실사 체크리스트는 `/diligence/<기업>`에 있고, 기업에게 주는 링크(`/deal/<기업>`) 바로 아래에 두지 않았습니다. 넘겨준 링크 바로 옆에 있는 페이지는 절반쯤 넘겨준 것이나 마찬가지입니다. 검색엔진에도 노출되지 않도록(noindex) 처리했습니다.

**(2주차 갱신) 이제는 정리정돈이 아니라 실제로 막혀 있습니다.** 위 문단은 로그인이 없던 1주차 기준이었습니다. 지금은 **사이트 전체가 로그인 뒤에** 있고, 주소를 알아도 로그인 없이는 열리지 않습니다.

**로그인은 화면마다 붙이지 않고 입구 한 곳에서 막습니다.** 화면마다 "로그인했나?" 확인을 넣으면, 새 화면을 만들 때 한 번 잊는 순간 그 화면만 뚫립니다. 그래서 **모든 요청이 지나가는 한 파일**(`proxy.ts`)에서 막고, 로그인 없이 통과시킬 주소만 이름을 적어 예외로 뒀습니다. 기본값이 "막힘"이면 잊어서 뚫리는 일이 없습니다.

**AI에게 서류 전문을 통째로 보내지 않습니다.** 실사 항목마다 **그 항목에 필요한 서류만** 골라 보냅니다(`relatedDocumentIds`). 그리고 한 번에 보내는 총량에 상한(16MB)을 둬서, 서류가 많은 기업에서 요금이 폭발하지 않게 했습니다.

**같은 서류를 13번 읽지 않습니다.** 정관은 실사 항목 5개에, 등기부등본은 4개에 쓰입니다. 60초 동안은 한 번 읽은 파일을 재사용합니다.

**코멘트는 한 개당 파일 하나입니다.** 한 파일에 전부 모아 저장하면, 두 사람이 동시에 코멘트를 쓸 때 **나중에 저장한 쪽이 앞사람 글을 지웁니다.** 여러 명이 같이 쓰는 기능이라 처음부터 이렇게 만들었습니다.

**계약서 작성 내용은 사람별이 아니라 기업별입니다.** 내가 작성하던 계약서를 검토할 동료가 볼 수 없으면 쓸모가 없습니다.

**기업은 자기 폴더에만 올리고, 자기 폴더에서만 지울 수 있습니다.** 삭제할 때 파일 경로를 서버가 직접 만들고, 파일명에 `/`가 들어 있으면 거부합니다. 이게 없으면 `../../diligence/zest.json` 같은 파일명으로 **실사 메모를 지울 수** 있습니다.

**서류가 도착했는지 판단하는 로직은 한 곳에만 있습니다.** `lib/deal-status.ts`를 두 화면이 공유합니다. 두 화면이 서로 다른 답을 내놓을 수가 없습니다.

**AI는 제안만 하고 체크하지 않습니다.** 파일명만 보고 "이건 등기부등본 같다"고 90% 확신하더라도, 그것만으로 법적 서류를 '접수됨'으로 처리하지 않습니다. 사람이 보고 판단합니다.

**AI가 없는 서류를 만들어낼 수 없습니다.** 응답 형식을 JSON 스키마로 강제하지만, 스키마는 **모양만** 강제하지 내용의 진위는 보장하지 않습니다. 그래서 AI가 돌려준 서류 id가 실제 체크리스트에 있는지 한 번 더 대조하고, 없으면 버립니다. 확신도 60% 미만도 숨깁니다.

**AI는 키워드가 실패한 파일만 봅니다.** 키워드 매칭이 먼저 돌고 항상 우선합니다. AI는 남은 것만 처리합니다.

**실사 항목마다 필요한 서류가 표시됩니다.** '발행주식수 일치' 항목 아래에 등기부등본·주주명부가 태그로 붙고, 제출됐으면 초록색입니다.

**멘토님의 💡 Tip을 전부 살렸습니다.** 교수 자문료 이슈, 마이너스 통장 엄금, 차입금은 투자금으로 상환 불가 — 잊어버리기 쉬운데 중요한 내용들입니다.

**저장소는 private입니다.** 실제 기업 서류가 올라가는 곳이라, 주소를 안다고 열리지 않습니다.

---

## 7. 보안 / Security notes

- `service-account.json`과 `.env.local`은 `.gitignore`에 등록되어 있습니다. **절대 커밋하면 안 됩니다.**
- **API 키를 채팅창에 붙여넣지 마세요.** 파일에 직접 넣거나 Vercel 대시보드에서 입력합니다.
- Vercel Blob 저장소는 `--access private`으로 만들었습니다.
- 드라이브는 `drive.readonly` 권한만 요청했습니다. 앱이 실수로 파일을 지울 수 없습니다.
- 파일 삭제 API는 경로 조작(`../`)을 차단합니다.
- 실제 기업 서류는 민감정보이므로 익명화된 샘플만 사용합니다.
- 이 문서에는 드라이브 폴더 ID와 API 키를 적지 않았습니다.
- **사이트 전체가 로그인 뒤에 있습니다.** 화면은 로그인 페이지로 보내고(302), API는 거부합니다(401).
- **구글이 이메일을 확인해 준 계정만** 통과합니다. 도메인만 보고 통과시키면, 확인되지 않은 주소를 스스로 적어 넣은 계정이 들어올 수 있습니다.
- 비밀번호는 **받지도 저장하지도 않습니다.** 구글이 본인 확인을 하고, 우리는 결과만 받습니다. 직원 비밀번호를 우리 쪽에 두지 않는 것이 목적이었습니다.
- 채운 계약서(`*-filled-agreement.docx`)도 `.gitignore`에 넣었습니다. **테스트로 채운 계약서가 커밋되면 안 됩니다.**

### 실제로 있었던 키 노출 사고 (2026-08-11)

**무슨 일이 있었나:** Blob 저장소를 만들려고 `vercel blob create-store`를 실행했는데, 이 명령이 **알리지 않고 `vercel env pull`도 같이 실행**했습니다. `env pull`은 Vercel에 저장된 모든 환경변수를 `.env.local`로 내려받는 명령입니다. 그 과정에서 구글 서비스 계정의 **비공개 키가 터미널에 출력**됐고, 그대로 작업 기록(로그 파일)에 남았습니다.

**어디까지 퍼졌나 (확인 결과):**

| 위치 | 상태 |
|---|---|
| 로컬 대화 기록 파일 (`.jsonl`) | 1건 남아 있음 |
| git 커밋 이력 | **없음** — 전체 커밋 확인 완료 |
| GitHub 등 외부 | **없음** — 이 저장소는 remote가 없어 push된 적이 없음 |

**위험도: 낮음.** 이유 세 가지 —

1. 노트북 밖으로 나간 적이 없습니다.
2. 이 키는 `drive.readonly` 권한뿐이라 파일을 읽을 수만 있고 지우거나 고칠 수 없습니다.
3. 드라이브 연동은 현재 꺼져 있어(`readsSampleDriveFolder: false`) 이 키를 쓰는 코드가 없습니다.

**조치: 재발급이 아니라 삭제.** 지금 아무 데도 안 쓰는 키라 새로 만들 이유가 없습니다. Google Cloud Console → IAM 및 관리자 → 서비스 계정 → 키 → 삭제. **없는 키는 유출될 수 없습니다.** 나중에 드라이브 연동을 다시 켤 일이 생기면 그때 새로 만들면 됩니다.

**→ 2026-08-13 삭제 완료.** 기록 파일에 남아 있던 그 값은 이제 아무 것도 열지 못합니다.

**교훈 — 같은 상황을 두 번째에는 피했습니다.** 며칠 뒤 OpenRouter 키를 받았을 때는 방식을 바꿨습니다. 키를 대화창에 넣지 않고 **본인이 직접** 메모장과 Vercel 대시보드에 입력했고, 나중에 변수 이름을 확인할 때도 각 줄을 `=` 기준으로 잘라 **왼쪽(이름)만** 출력했습니다. 확인 결과 OpenRouter 키는 기록 파일에 **0건**입니다.

> **규칙: 키는 "어느 파일에 넣으세요"라고 알려주고, 값 자체는 절대 받지도 출력하지도 않습니다.**

---

## 8. 확인한 것 / What has actually been tested

- 브라우저에서 파일 끌어다 놓기 → 업로드 → 체크 표시까지 동작 확인
- 파일 삭제 → 체크 해제까지 확인
- 삭제 API에 `../diligence/zest.json` 넣어보기 → **400 거부 확인**
- AI 추정: `제스트_등기_최신본_v3.pdf` → 등기부등본 90% (로컬)
- AI 추정: `주주_명단_최종.xlsx` → 주주명부 95% (배포된 사이트)
- 실사 체크·메모 저장 후 새로고침 → 그대로 남아 있음 확인
- 메모만 수정했을 때 체크박스가 풀리지 않는지 확인
- 없는 실사 항목 / 없는 기업으로 요청 시 거부되는지 확인
- 한글 저장·조회 정상 확인

**2주차 추가 확인:**

- 로그인 안 한 상태로 모든 주소 접근 → 화면은 302(로그인으로), API는 401 **전부 확인**
- 계약서 화면·API도 같이 확인 (`/agreement/zest` → 302, 계약서 API 2개 → 401)
- ZIP 압축 풀기: 실제 한국 윈도우에서 만든 ZIP으로 확인 — **한글 파일명 안 깨짐**
- AI 서류 분석: 실제 스캔된 한글 PDF로 확인 — 읽고 판단까지 동작
- 확장자 없는 파일(`...pdf의 사본`) → 내용으로 PDF 판별되는지 확인
- 계약서 채우기: 실제 양식으로 채워서 **Word에서 열림 확인**, 회사명·주식수·금액 한글 표기 들어감, `&` 처리됨, 위약벌 12% 들어감, **노란 편집 표시 남아 있지 않음**
- 계약서 양식 자리 85곳 ↔ 입력칸 57개 대응 검사 — **빠짐·중복 없음**
- 배포 후 실제 주소에서 재확인

**사람이 브라우저에서 확인 완료 (2026-08-13):**

- 기업 추가 / 보관 / 완전 삭제
- 배치 만들고 기업 배치에 넣기
- 코멘트 쓰기·지우기 — **남의 코멘트는 지워지지 않음** 확인
- 계약서 입력 → 저장 → 다른 계정으로 로그인해도 그대로 보임 확인
- 계약서 .docx / .pdf 내려받기
- 사이드바 단계 필터 — 각 단계 선택·해제, 실시간 카운트
- 계약서 화면 AI 자동 입력 — 항목별 적용 칩, 전체 적용 버튼

---

## 9. 로그인은 어떻게 만들었나 / How the login works

멘토님 요구: **스파크랩스 직원만 들어올 수 있게.** 제 질문은 "직원 비밀번호를 우리가 받아야 하나요?"였고, 답은 **받지 않아도 됩니다.**

**방식: 구글 로그인 + 도메인 확인.** 직원은 "구글로 로그인" 버튼을 누르고, 이미 로그인된 회사 계정을 고릅니다. 구글이 "이 사람은 `누구@sparklabs.co.kr` 맞다"고 알려주고, 우리는 **주소 뒷부분이 `@sparklabs.co.kr`인지만 확인**합니다. 비밀번호는 우리 쪽을 지나가지도 않습니다.

**직접 만들면서 알게 된 함정 두 개 —**

1. **`hd` 옵션은 보안 장치가 아닙니다.** 구글에 "이 도메인만"이라고 알려주는 옵션이 있는데, 이건 **계정 선택 화면에 힌트를 주는 것**일 뿐입니다. 실제 차단은 우리 서버에서 해야 합니다.
2. **이메일 확인 여부까지 봐야 합니다.** 도메인만 보면, 확인되지 않은 주소를 스스로 적어 넣은 계정이 통과할 수 있습니다. 그래서 구글이 "확인됨"이라고 표시한 계정만 통과시킵니다.

**예외 명단(`ALLOWED_EMAILS`)을 뒀습니다.** 저는 인턴이라 회사 계정이 없어서, 제 개인 계정을 명단에 넣었습니다. 예외가 있을 때는 위 `hd` 힌트를 빼야 합니다 — 힌트가 켜져 있으면 구글이 개인 계정을 아예 보여주지 않습니다.

---

## 10. 계약서 양식은 어떻게 채우나 / How the contract gets filled

**문제:** Word 파일은 겉보기엔 한 문장이어도, 안에서는 **한 글자씩 조각조각 나뉘어 저장**되어 있습니다. `[이억이천사백사만육천칠백육십]` 한 칸이 6조각으로 쪼개져 있기도 합니다. 이걸 요청이 올 때마다 조립해서 채우면 느리고, **법적 문서를 망칠 위험**이 있습니다.

**해결: 어려운 일은 한 번만 미리 해둡니다.** `scripts/prepare-template.py`를 **딱 한 번 돌려서**, 노란색으로 칠해진 자리를 찾아 조각을 합치고 `{{f1}}` 같은 표시 하나로 바꿔 놓은 양식을 만들었습니다. 그 뒤로는 채우는 일이 **글자 바꾸기**라서 간단하고 안전합니다. 노란 형광펜도 이때 다 지웠습니다 — 완성된 계약서에 편집 표시가 남아 있으면 안 됩니다.

**멘토님 지시로 표준을 고정한 값:** 위약벌 **12%**, 퇴사제한 **5년**. 기본값으로 넣어두고, 누가 다른 값으로 바꾸면 입력칸에 **주황색 테두리**가 생겨 "표준과 다릅니다"라고 알립니다. 계약서 부수는 보통 **3부**(스파크랩스 / 회사 / 대표), 이해관계인이 더 있으면 늘립니다.

**중간에 나온 문제 세 개 —**

1. **연도가 고정되어 있었습니다.** 양식에 `2026년`이 그냥 글자로 적혀 있었고 노란색도 아니어서, 첫 작업에서 그냥 지나갔습니다. 그러면 내년 계약서를 이 사이트로 못 만듭니다. 재무제표 쪽은 `202X년`으로 비어 있었습니다. → 연도도 채울 수 있게 만들었습니다. 다만 **번호를 뒤에 이어 붙였습니다**(78번부터). 중간에 끼워 넣으면 그 뒤 번호가 전부 한 칸씩 밀려서, 연결표 77개가 한꺼번에 틀어집니다.
2. **`&` 하나 때문에 Word가 파일을 거부합니다.** 회사명에 `&`가 들어가면 Word가 "손상된 파일"이라고 합니다. Word 내부는 XML이라 `&`가 특수문자입니다. → 값을 넣을 때 `& < > " '`를 변환합니다.
3. **양식 안의 회사 정보가 스파크랩스 조합 정보였습니다.** 투자받는 회사는 서명란에 나옵니다. **멘토님 확인 완료 (2026-08-13): 이것이 맞습니다.** '회사' 항목은 의도적으로 스파크랩스 조합 정보를 담고 있으며, 투자받는 기업이 아닙니다. 계약서 화면에서 편집할 수 있습니다.

**빈 칸은 비워두지 않고 `{{f27}}` 그대로 남깁니다.** 그냥 비우면 "일부러 비운 조항"처럼 읽힙니다. 표시가 남아 있으면 안 채운 게 눈에 보입니다. 몇 개가 안 채워졌는지도 화면에 알려줍니다.

**.pdf는 브라우저 인쇄 기능을 씁니다.** `.docx`는 원본 양식 그대로라 서식이 100% 정확합니다. PDF는 미리보기를 인쇄하는 방식이라 **글자 배치가 조금 다를 수 있습니다.** 도장 찍어 제출할 파일은 `.docx`를 쓰고, PDF는 공유·확인용으로 보는 것이 안전합니다. 완전히 똑같은 PDF가 필요하면 변환 서비스를 붙이면 됩니다.

---

## 11. 다음 단계 / What's next

**1주차 3개 과제 완료:**

- [x] 서류 트래커 미니과제
- [x] CRUD 1개 — 업로드(C) / 목록(R) / 실사 메모 수정(U) / 파일 삭제(D)
- [x] Claude API 연동 1개 — OpenRouter 경유, 파일명 자동 추정

**2주차 완료:**

- [x] 딜 CRUD — 코드가 아니라 화면에서 기업 추가 / 보관 / 완전 삭제
- [x] 배치 만들기 + 기업을 배치에 넣기
- [x] 홈 화면 진행 현황 + 사이드바
- [x] 실사 항목별 코멘트 (여러 명이 같이)
- [x] 로그인 — 사이트 전체를 스파크랩스 계정만
- [x] AI 크로스체크 — 파일명이 아니라 **서류 내용**을 읽어 항목별로 판단
- [x] ZIP으로 받은 서류 압축 풀기
- [x] 계약서 자동 작성 + .docx / .pdf 내려받기
- [x] 서비스 계정 키 삭제 (위 §7)

**2주차 이후 추가:**

- [x] 사이드바 단계 필터 — 서류 수집 중 / 실사 중 / 계약 준비, 단계별 기업 수 실시간 표시
- [x] 계약서 화면 AI 자동 입력 제안 — 업로드 서류 기반, 회색 흐린 글씨로 제안, 항목별 적용 칩 + 전체 적용 버튼
- [x] 저장 버튼 / PDF 저장 버튼 시각적 구분 (초록색 채우기 vs. 테두리만)
- [x] OpenRouter → Google Gemini 직접 API 전환 (같은 모델 `gemini-2.5-flash-lite`, 중개소 제거)
- [x] 전 기능 브라우저 테스트 완료 (§8)
- [x] '회사' 항목 멘토님 확인 완료 (§10-3)
- [x] GitHub 푸시 완료

**남은 것:**

- [ ] 예비실사 체크리스트(사내 표준 양식) 확보 후 반영
- [ ] 서식이 완전히 동일한 PDF가 필요하면 변환 서비스 연결 (위 §10)
- [ ] Vercel 대시보드에서 구 `OPENROUTER_API_KEY` 삭제 (`GOOGLE_AI_API_KEY`로 교체됨)

---

## 12. 다음 기수 인턴에게 / For the next intern

1. **에러 메시지는 그대로 복사해서 붙여넣으세요.** 요약하지 마세요. 못생긴 빨간 글자 안에 답이 있습니다.
2. **멘토님 피드백도 그대로 옮기세요.** 요약하면 뉘앙스가 날아갑니다.
3. **진짜 데이터를 먼저 보세요.** 문서만 보고 만들면 ①번 같은 버그가 납니다.
4. **다만 데이터의 모든 패턴이 의미 있는 건 아닙니다.** 저는 번호가 비어 있는 걸 보고 혼자 추측해서 기록까지 남겼는데, 그냥 샘플을 몇 개 뺀 것이었습니다. 추측하지 말고 물어보세요.
5. **화면이 이상하면 바로 말하세요.** "왜 이게 체크되어 있어?" 한마디로 버그를 잡았습니다.
6. **고를 일이 생기면 재보세요.** AI 모델을 감으로 고르지 않고 테스트를 만들어서 비교했더니, 제일 비싼 모델과 제일 싼 모델의 답이 똑같았습니다. 13배 차이였습니다. 측정하는 데 10분 걸렸습니다.
7. **AI가 자신 있게 틀립니다.** "진짜 테스트해봤어?"라고 물어보세요. 실제로 돌려서 확인하기 전까지는 완성이 아닙니다. 이건 우리가 만든 AI 기능에도 똑같이 적용됩니다 — 그래서 AI 추정 결과는 사람이 확인하도록 만들었습니다.
8. **"안 된다"가 "실패했다"는 뜻은 아닙니다.** ZIP 업로드가 안 된다고 했을 때, 업로드는 완벽하게 되고 있었습니다. 압축을 푸는 기능이 없었을 뿐입니다. **어디까지 됐는지 먼저 확인**하면 엉뚱한 곳을 고치지 않습니다.
9. **똑같아 보이는데 안 되면 눈에 안 보이는 문자를 의심하세요.** 이 프로젝트에서 세 번 겪었습니다. 사람 눈으로 대조하는 걸 그만두고 컴퓨터가 실제로 보내는 값을 확인해야 잡힙니다.
10. **보안은 "내가 막았다고 생각한 것"과 다릅니다.** 저는 코멘트를 자기 것만 지우게 만들었다고 생각했지만, 실제로는 브라우저가 "저는 아무개입니다"라고 주장하는 걸 그대로 믿고 있었습니다. **브라우저에서 오는 값은 전부 거짓일 수 있습니다.**
11. **갈아엎는 걸 무서워하지 마세요.** 1차 버전을 통째로 바꿨지만, 그 과정에서 배운 게 전부 2차 버전에 들어갔습니다. 코드는 git에 남아 있어서 언제든 되돌릴 수 있습니다.
