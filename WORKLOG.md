# 작업 기록 — 서류 취합 트래커 + 서류 실사 체크리스트

**Work log — Document Collection Tracker & Due Diligence Checklist**

작성자 / Author: Evan Kang
날짜 / Date: 2026-08-12
배포 링크 / Live app: https://sparklabs-doc-tracker.vercel.app

> 다음 기수 인턴을 위한 참고 자료입니다. 프롬프트 원문과 막혔던 지점을 그대로 남겼습니다.
> Written for the next intern. The prompts and the dead ends are recorded as they actually happened.

---

## 1. 무엇을 만들었나 / What this is

두 개의 화면입니다.

**① 서류 취합 트래커 (기업용)** — `/deal/<기업>`
투자 전 제출 서류 목록을 보여주고, **기업이 직접 자기 서류를 업로드**합니다. 파일이 올라오면 파일명으로 어떤 서류인지 자동 판별해서 체크가 됩니다. 잘못 올린 파일은 지울 수 있고, 파일명으로 판별하지 못한 파일은 **AI가 어떤 서류인지 추정**해 줍니다. 기업별로 링크가 따로 있어서, 그 링크만 전달하면 됩니다.

**② 서류 실사 체크리스트 (내부용)** — `/diligence/<기업>`
제출된 서류들을 서로 대조하는 실사 항목 13개. 각 항목마다 체크박스와 메모가 있고 자동 저장됩니다.

### 서류 목록 / The checklist

- 국내 기업: 필수 18건 + 해당 시 3건
- 해외 기업: 필수 10건 + 해당 시 1건

두 리스트는 번역 관계가 아니라 **내용이 실제로 다릅니다.** (예: 해외는 Cap Table이 필수, 국내는 4대보험 가입자명부가 필수)

### 실사 항목 / Due diligence items

멘토님의 「#3. 서류 실사」 문서 13개 항목을 두 그룹으로 나눴습니다.

- **서류 검증 (1~7번)** — 사업목적 일치, 발행주식수 일치, 정관 종류주식·신주인수권, 옵션풀, 등기 순서, 차입금·가수금, 주주 구성, 지식재산권, 최종 대조
- **후속 절차 (8~13번)** — 서면 communication, 예비실사 고지, 계약서 작성, 계약 체결, 재무팀 공유, 투자심사보고서

### 기술 스택 / Stack

| | |
|---|---|
| Framework | Next.js 16.3 (App Router) + React 19.2 |
| Language | TypeScript |
| Styling | Tailwind CSS v4 |
| 파일 저장 | Vercel Blob (private) |
| AI | OpenRouter → `google/gemini-2.5-flash-lite` |
| 배포 | Vercel |

### 파일 구조 / Where the code lives

| 파일 | 역할 |
|---|---|
| `lib/documents.ts` | 필수 서류 목록 + 파일명 매칭. **여기만 고치면 서류 목록이 바뀝니다.** |
| `lib/diligence.ts` | 실사 항목 13개 정의 |
| `lib/deals.ts` | 기업 목록. 2주차에 CRUD로 대체 예정 |
| `lib/deal-status.ts` | 어떤 서류가 도착했는지 계산 (두 화면이 공유) |
| `lib/storage.ts` | 업로드된 파일 목록 조회·삭제 |
| `lib/diligence-store.ts` | 실사 체크·메모 저장 |
| `lib/classify.ts` | AI 파일명 추정 (OpenRouter 호출) |
| `app/deal/[dealId]/` | 기업용 업로드 화면 |
| `app/diligence/[dealId]/` | 내부용 실사 화면 |
| `app/api/upload/` | 업로드 토큰 발급 |
| `scripts/bench-models.mjs` | AI 모델 비교 테스트 (아래 ⑨ 참고) |

---

## 2. 중간에 통째로 갈아엎은 것 / The rewrite

**1차 버전은 방향이 틀렸습니다.** 처음에는 멘토님의 구글 드라이브 샘플 폴더를 앱이 읽어서, 그 폴더 상태를 화면에 보여주는 구조였습니다. 데모로는 그럴듯했지만 실무에서 쓸 수 없는 물건이었습니다.

멘토님 피드백 세 가지:

1. **기업이 직접 올릴 수 있어야 한다.** 우리가 폴더를 들여다보는 게 아니라, 기업별로 링크를 주고 기업이 업로드하는 구조여야 합니다.
2. **서류 목록이 틀렸다.** 9건이 아니라 국내 18건 / 해외 10건입니다.
3. **파일 번호의 6번, 9번이 비어 있던 건 아무 의미가 없다.** 샘플 배치를 만들면서 몇 개를 뺀 것뿐입니다.

3번이 특히 뼈아팠습니다. 저는 **1차 작업 기록에 "6번과 9번이 무슨 서류인지 확인 필요"라고 적어놨었습니다.** 데이터에 있는 패턴이 전부 의미 있는 건 아닙니다. 추측을 기록으로 남기기 전에 물어봤어야 했습니다.

### ⚠️ 과업지시서와 다르게 만든 부분 (멘토님 지시)

**과업지시서 원문 (3.미니과제 탭, 핵심 기능 2번):**

> **구글 드라이브 업로드 인식** — 멘토가 미리 만들어둔 드라이브 폴더에 서류 파일을 올리면, 웹 앱이 '어떤 서류가 올라왔는지' 파일명으로 판별

**실제로 만든 것:** 기업별 링크를 통한 **직접 업로드**. 앱이 드라이브 폴더를 들여다보는 방식이 아닙니다.

**이유:** 멘토님이 구두로 다르게 지시하셨습니다. "파일을 올리는 사람이 웹사이트에 직접 올리도록 유도하는 형태여야 한다, 지금처럼 우리가 특정 폴더를 보고 결과를 보여주는 방식이 아니라 **사용자별(기업별) 케이스**로 동작해야 한다."

즉 **문서에는 드라이브 인식이라고 적혀 있지만, 멘토님은 직접 업로드를 요구하셨고 그쪽을 따랐습니다.** 실무에서는 우리가 기업 폴더를 들여다보는 게 아니라 기업이 자기 서류를 제출하는 것이므로, 구두 지시 쪽이 실제 업무 흐름에 맞습니다.

**드라이브 연동 코드는 지우지 않고 꺼두었습니다** (`lib/drive.ts`, `readsSampleDriveFolder: false`). 원래 요구사항대로 되돌려야 할 일이 생기면 스위치만 켜면 됩니다.

**교훈:** 문서와 사람 말이 다르면 사람 말이 최신입니다. 다만 **왜 다르게 만들었는지는 반드시 기록**해 두어야, 나중에 "요구사항 2번은 왜 안 했냐"는 질문에 답할 수 있습니다.

**갈아엎으면서 얻은 것:** 드라이브 연동 코드(`lib/drive.ts`)는 지우지 않고 스위치만 꺼뒀습니다(`readsSampleDriveFolder: false`). 나중에 필요하면 다시 켜면 됩니다. git에도 남아 있습니다.

---

## 3. 어떻게 만들었나 / How it was built

전부 Claude Code(터미널에서 실행되는 AI 코딩 도구)와 대화하면서 만들었습니다. 저는 코딩 경험이 없는 상태로 시작했습니다.

**가장 중요한 교훈: 한 번에 완성되는 큰 프롬프트는 없었습니다.** 짧은 요청을 계속 주고받는 방식이 훨씬 효과적이었습니다.

### 실제로 사용한 프롬프트 / The prompts I actually used

과제 파악:

```
can you find the original korean file for me and just translate it
```

→ 영어 버전 파일도 있었지만 불완전해서, 한글 원본을 직접 읽게 하는 편이 정확했습니다.

방향 수정 (가장 중요했던 프롬프트):

```
ok so basically, what we have right now is not good enough. i talked with my
mentor. he said that firstly, we want it to be something where the person who
is uploading these files can be prompted to upload their files onto the
website, so that it is user by user case, not like right now where we just
went and showed our results to our specific file. also, our list of documents
needed was incorrect. we needed much more, ill attach the list.
```

→ **멘토님 말씀을 정리하지 말고 그대로 옮기는 게 나았습니다.** 요약하면 뉘앙스가 날아갑니다.

검증:

```
hey so where did you get the info regarding zest, and why is it checking off
which documents we apparently have without the user having uploaded a file
```

→ 화면이 이상하면 바로 물어보세요. 실제로 버그였습니다. (아래 ②번)

모델 선택:

```
we have an openrouter api that was given to me to find the most cost efficient
and best fitting api for our tasks. so find the best one please.
```

→ "제일 좋은 거 골라줘"가 아니라 **기준(비용·적합성)을 같이 주면** 추측 대신 실제 측정을 하게 됩니다.

이해가 안 될 때:

```
im kinda lost, what did you just do. explain it to me like im 5
```

---

## 4. 막혔던 지점과 해결 / What went wrong, and how it got fixed

평가 기준이 "결과보다 과정"이라고 되어 있어서, 실제로 막혔던 부분을 전부 남깁니다.

### ① 서류 이름이 두 가지였음 — 가장 중요한 버그

드라이브 폴더의 파일은 `등기사항전부증명서`인데, 용어집에는 `법인 등기부등본`으로 적혀 있었습니다. 같은 서류의 다른 이름입니다.

처음 만든 매칭 로직은 `등기부등본`만 찾고 있어서, **파일이 바로 거기 있는데도 "미비"로 표시**됐을 상황이었습니다.

**해결:** 서류 하나에 여러 개의 이름(별칭)을 등록할 수 있게 만들었습니다.

```ts
keywords: ["등기사항전부증명서", "등기사항증명서", "등기부등본", "등기부", ...]
```

**교훈:** 실제 파일을 먼저 확인하지 않고 용어집만 보고 만들었으면 데모에서 틀렸을 겁니다. 코드를 짜기 전에 진짜 데이터를 먼저 보세요.

### ② 아무것도 안 올렸는데 체크가 되어 있었음

새 업로드 화면을 열었더니 **파일을 하나도 안 올렸는데 서류 몇 개가 이미 체크**되어 있었습니다. 멘토님 샘플 드라이브 폴더를 아직 읽고 있었기 때문입니다.

기업 입장에서는 "내가 안 냈는데 냈다고 나오는" 화면입니다. 스위치를 꺼서 해결했습니다.

**교훈:** 화면에 이상한 게 보이면 그냥 넘어가지 마세요. 물어본 덕분에 잡았습니다.

### ③ 4.5MB보다 큰 파일

IR덱은 보통 15~20MB입니다. (제스트 IR덱은 17MB) 그런데 서버를 거쳐 업로드하면 4.5MB에서 막힙니다.

**해결:** 브라우저가 서버를 거치지 않고 저장소에 직접 올리는 방식(client upload)으로 바꿨습니다. 서버는 "이 경로에 올려도 된다"는 토큰만 발급합니다. 한도는 50MB로 잡았습니다.

### ④ 환경변수가 저장되지 않았음

Vercel 대시보드에서 환경변수를 입력했는데 저장이 안 됐습니다. 화면상으로는 알 수 없었고, 배포된 앱이 계속 샘플 데이터를 보여줬습니다.

```
npx vercel env ls
```

**교훈:** 환경변수는 **저장한 이후에 만든 배포부터** 적용됩니다. 저장했으면 반드시 다시 배포해야 합니다.

### ⑤ 눈에 안 보이는 문자 때문에 인증 실패

환경변수 값을 복사해서 붙여넣었더니 맨 앞에 눈에 보이지 않는 문자(BOM)가 딸려 들어갔습니다. 화면상으로는 완전히 똑같아 보이는데 인증만 실패합니다.

**해결:** 값을 읽을 때 앞뒤 공백을 잘라내도록(`.trim()`) 했습니다.

### ⑥ 환경변수 이름에 띄어쓰기

OpenRouter 키를 `.env.local`에 넣을 때 이름을 `OPENROUTER API KEY`로 적었습니다. **환경변수 이름에는 띄어쓰기가 들어갈 수 없습니다.** `OPENROUTER_API_KEY`로 고쳐야 합니다.

에러 메시지가 "키가 없다"고만 나오기 때문에, 파일에 분명히 키가 있는데도 원인을 찾기 어렵습니다.

**교훈:** 환경변수 이름은 **대문자와 밑줄(`_`)만** 씁니다.

### ⑦ 윈도우 PowerShell에서 npx 실행 불가

```
npx : File ...\npx.ps1 cannot be loaded because running scripts is disabled
```

**해결:** `npx` 대신 `npx.cmd`를 쓰거나, 아래 명령으로 한 번만 설정하면 됩니다.

```powershell
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
```

### ⑧ `!`는 PowerShell 명령이 아님

Claude Code 입력창에서 `! notepad .env.local`처럼 앞에 `!`를 붙이면 "이건 AI 말고 터미널이 바로 실행해"라는 뜻입니다. 그런데 이걸 **PowerShell 창에 그대로 치면** 에러가 납니다. PowerShell에서 `!`는 '아니다(not)'라는 뜻이기 때문입니다.

| 어디에 치는가 | 어떻게 치는가 |
|---|---|
| PowerShell (`PS C:\...>`) | `notepad .env.local` |
| Claude Code 입력창 | `! notepad .env.local` |

### ⑨ 테스트했더니 한글이 `???`로 나옴 — 앱 문제가 아니었음

실사 메모에 한글을 저장하고 다시 읽었더니 `???`가 나왔습니다. 앱 버그처럼 보였지만, PowerShell이 요청을 보낼 때 한글을 깨뜨린 것이었습니다. UTF-8로 명시해서 다시 보내니 정상이었습니다.

**교훈:** 테스트가 실패하면 **테스트 도구부터 의심하세요.** 앱을 고치기 전에 확인해야 합니다.

---

## 5. AI 모델은 어떻게 골랐나 / How the AI model was chosen

회사에서 OpenRouter API 키를 받았습니다. **OpenRouter는 중개소입니다** — 키 하나로 구글·OpenAI·Anthropic 등 400개 이상의 모델을 다 쓸 수 있습니다. 회사별로 따로 가입하고 따로 결제할 필요가 없습니다.

과제는 "가장 저렴하면서 우리 일에 잘 맞는 모델 찾기"였습니다. 감으로 고르지 않고 **직접 테스트를 만들어서 비교했습니다** (`scripts/bench-models.mjs`).

테스트 문제는 **키워드 매칭이 실제로 실패하는 파일명 6개**입니다. 예: `제스트_등기_최신본_v3.pdf`(등기부등본), `주주_명단_최종.xlsx`(주주명부). 마지막 한 개는 `IMG_4821.jpg` — **정답이 "모르겠다"인 문제**입니다. 이걸 자신 있게 찍는 모델은 오히려 위험합니다.

결과 (2026-08-12 기준, 실제 토큰 사용량으로 계산):

| 모델 | 정답 | 소요시간 | 1회 비용 | $1로 몇 번 |
|---|---|---|---|---|
| **google/gemini-2.5-flash-lite** | 6/6 | 2.9초 | **$0.000233** | **4,301회** |
| google/gemini-3.1-flash-lite | 6/6 | 2.1초 | $0.000683 | 1,464회 |
| openai/gpt-5-mini | 6/6 | 9.9초 | $0.002321 | 431회 |
| anthropic/claude-haiku-4.5 | 6/6 | 6.7초 | $0.003187 | 314회 |

**넷 다 6/6이었습니다.** 이 정도 난이도에서는 성능 차이가 안 납니다. 그래서 **성능이 같으면 가격으로 결정**했습니다. gemini-2.5-flash-lite는 claude-haiku-4.5보다 **13배 저렴**하면서 답은 똑같았습니다.

솔직한 한계:

- 문제가 6개뿐이라 모델을 강하게 구분하지는 못합니다.
- gemini-2.5-flash-lite는 맞힌 문제에 전부 확신도 `1.0`을 줬습니다. 확신도 눈금이 거친 편입니다. 다만 모르는 문제(`IMG_4821.jpg`)에는 `0.1`을 줬으므로, 중요한 구분은 됩니다.
- 더 정교한 확신도가 필요하면 gemini-3.1-flash-lite가 나았습니다. 3배 비싸지만 여전히 1회 0.07센트입니다.

**모델 교체는 코드 수정 없이 환경변수 한 줄입니다:** `OPENROUTER_MODEL=...`. 바꾸기 전에 벤치마크를 다시 돌리세요.

> 참고: 과업지시서에는 "Claude API 연동"이라고 되어 있습니다. OpenRouter는 Claude도 포함하므로, `OPENROUTER_MODEL=anthropic/claude-haiku-4.5`로 바꾸면 그대로 Claude 연동이 됩니다. 같은 답을 13배 비싸게 받는 것이라 기본값은 Gemini로 뒀습니다.

---

## 6. 설계에서 신경 쓴 점 / Design decisions worth keeping

**기업용 화면과 내부용 화면이 분리되어 있습니다.** 실사 체크리스트는 `/diligence/<기업>`에 있고, 기업에게 주는 링크(`/deal/<기업>`) 바로 아래에 두지 않았습니다. 넘겨준 링크 바로 옆에 있는 페이지는 절반쯤 넘겨준 것이나 마찬가지입니다. 검색엔진에도 노출되지 않도록(noindex) 처리했습니다.

**다만 이건 정리정돈이지 보안이 아닙니다.** 주소를 아는 사람은 여전히 열 수 있습니다. 제대로 된 로그인은 아직 없습니다.

**기업은 자기 폴더에만 올리고, 자기 폴더에서만 지울 수 있습니다.** 삭제할 때 파일 경로를 서버가 직접 만들고, 파일명에 `/`가 들어 있으면 거부합니다. 이게 없으면 `../../diligence/zest.json` 같은 파일명으로 **실사 메모를 지울 수** 있습니다.

**서류가 도착했는지 판단하는 로직은 한 곳에만 있습니다.** `lib/deal-status.ts`를 두 화면이 공유합니다. 두 화면이 서로 다른 답을 내놓을 수가 없습니다.

**AI는 제안만 하고 체크하지 않습니다.** 파일명만 보고 "이건 등기부등본 같다"고 90% 확신하더라도, 그것만으로 법적 서류를 '접수됨'으로 처리하지 않습니다. 사람이 보고 판단합니다.

**AI가 없는 서류를 만들어낼 수 없습니다.** 응답 형식을 JSON 스키마로 강제하지만, 스키마는 **모양만** 강제하지 내용의 진위는 보장하지 않습니다. 그래서 AI가 돌려준 서류 id가 실제 체크리스트에 있는지 한 번 더 대조하고, 없으면 버립니다. 확신도 60% 미만도 숨깁니다.

**AI는 키워드가 실패한 파일만 봅니다.** 키워드 매칭이 먼저 돌고 항상 우선합니다. AI는 남은 것만 처리합니다.

**실사 항목마다 필요한 서류가 표시됩니다.** '발행주식수 일치' 항목 아래에 등기부등본·주주명부가 태그로 붙고, 제출됐으면 초록색입니다.

**멘토님의 💡 Tip을 전부 살렸습니다.** 교수 자문료 이슈, 마이너스 통장 엄금, 차입금은 투자금으로 상환 불가 — 잊어버리기 쉬운데 중요한 내용들입니다.

**저장소는 private입니다.** 실제 기업 서류가 올라가는 곳이라, 주소를 안다고 열리지 않습니다.

---

## 7. 보안 / Security notes

- `service-account.json`과 `.env.local`은 `.gitignore`에 등록되어 있습니다. **절대 커밋하면 안 됩니다.**
- **API 키를 채팅창에 붙여넣지 마세요.** 파일에 직접 넣거나 Vercel 대시보드에서 입력합니다.
- Vercel Blob 저장소는 `--access private`으로 만들었습니다.
- 드라이브는 `drive.readonly` 권한만 요청했습니다. 앱이 실수로 파일을 지울 수 없습니다.
- 파일 삭제 API는 경로 조작(`../`)을 차단합니다.
- 실제 기업 서류는 민감정보이므로 익명화된 샘플만 사용합니다.
- 이 문서에는 드라이브 폴더 ID와 API 키를 적지 않았습니다.

### 실제로 있었던 키 노출 사고 (2026-08-11)

**무슨 일이 있었나:** Blob 저장소를 만들려고 `vercel blob create-store`를 실행했는데, 이 명령이 **알리지 않고 `vercel env pull`도 같이 실행**했습니다. `env pull`은 Vercel에 저장된 모든 환경변수를 `.env.local`로 내려받는 명령입니다. 그 과정에서 구글 서비스 계정의 **비공개 키가 터미널에 출력**됐고, 그대로 작업 기록(로그 파일)에 남았습니다.

**어디까지 퍼졌나 (확인 결과):**

| 위치 | 상태 |
|---|---|
| 로컬 대화 기록 파일 (`.jsonl`) | 1건 남아 있음 |
| git 커밋 이력 | **없음** — 전체 커밋 확인 완료 |
| GitHub 등 외부 | **없음** — 이 저장소는 remote가 없어 push된 적이 없음 |

**위험도: 낮음.** 이유 세 가지 —

1. 노트북 밖으로 나간 적이 없습니다.
2. 이 키는 `drive.readonly` 권한뿐이라 파일을 읽을 수만 있고 지우거나 고칠 수 없습니다.
3. 드라이브 연동은 현재 꺼져 있어(`readsSampleDriveFolder: false`) 이 키를 쓰는 코드가 없습니다.

**조치: 재발급이 아니라 삭제.** 지금 아무 데도 안 쓰는 키라 새로 만들 이유가 없습니다. Google Cloud Console → IAM 및 관리자 → 서비스 계정 → 키 → 삭제. **없는 키는 유출될 수 없습니다.** 나중에 드라이브 연동을 다시 켤 일이 생기면 그때 새로 만들면 됩니다.

**교훈 — 같은 상황을 두 번째에는 피했습니다.** 며칠 뒤 OpenRouter 키를 받았을 때는 방식을 바꿨습니다. 키를 대화창에 넣지 않고 **본인이 직접** 메모장과 Vercel 대시보드에 입력했고, 나중에 변수 이름을 확인할 때도 각 줄을 `=` 기준으로 잘라 **왼쪽(이름)만** 출력했습니다. 확인 결과 OpenRouter 키는 기록 파일에 **0건**입니다.

> **규칙: 키는 "어느 파일에 넣으세요"라고 알려주고, 값 자체는 절대 받지도 출력하지도 않습니다.**

---

## 8. 확인한 것 / What has actually been tested

- 브라우저에서 파일 끌어다 놓기 → 업로드 → 체크 표시까지 동작 확인
- 파일 삭제 → 체크 해제까지 확인
- 삭제 API에 `../diligence/zest.json` 넣어보기 → **400 거부 확인**
- AI 추정: `제스트_등기_최신본_v3.pdf` → 등기부등본 90% (로컬)
- AI 추정: `주주_명단_최종.xlsx` → 주주명부 95% (배포된 사이트)
- 실사 체크·메모 저장 후 새로고침 → 그대로 남아 있음 확인
- 메모만 수정했을 때 체크박스가 풀리지 않는지 확인
- 없는 실사 항목 / 없는 기업으로 요청 시 거부되는지 확인
- 한글 저장·조회 정상 확인

---

## 9. 다음 단계 / What's next

**1주차 3개 과제 완료:**

- [x] 서류 트래커 미니과제
- [x] CRUD 1개 — 업로드(C) / 목록(R) / 실사 메모 수정(U) / 파일 삭제(D)
- [x] Claude API 연동 1개 — OpenRouter 경유, 파일명 자동 추정

**남은 것:**

- [ ] 2주차: 딜 CRUD — 코드가 아니라 화면에서 기업 추가
- [ ] 2주차: 예비실사 체크리스트(사내 표준 양식) 확보 후 반영
- [ ] 3주차: AI 크로스체크 — 파일명이 아니라 **서류 내용**을 읽어 숫자 불일치 탐지
- [ ] 3주차: 계약서 초안 자동 생성
- [ ] 로그인 기능 — 내부용 화면 보호
- [ ] 서비스 계정 키 삭제 (위 §7 사고 참고 — 재발급 불필요, 지금 안 쓰는 키임)

---

## 10. 다음 기수 인턴에게 / For the next intern

1. **에러 메시지는 그대로 복사해서 붙여넣으세요.** 요약하지 마세요. 못생긴 빨간 글자 안에 답이 있습니다.
2. **멘토님 피드백도 그대로 옮기세요.** 요약하면 뉘앙스가 날아갑니다.
3. **진짜 데이터를 먼저 보세요.** 문서만 보고 만들면 ①번 같은 버그가 납니다.
4. **다만 데이터의 모든 패턴이 의미 있는 건 아닙니다.** 저는 번호가 비어 있는 걸 보고 혼자 추측해서 기록까지 남겼는데, 그냥 샘플을 몇 개 뺀 것이었습니다. 추측하지 말고 물어보세요.
5. **화면이 이상하면 바로 말하세요.** "왜 이게 체크되어 있어?" 한마디로 버그를 잡았습니다.
6. **고를 일이 생기면 재보세요.** AI 모델을 감으로 고르지 않고 테스트를 만들어서 비교했더니, 제일 비싼 모델과 제일 싼 모델의 답이 똑같았습니다. 13배 차이였습니다. 측정하는 데 10분 걸렸습니다.
7. **AI가 자신 있게 틀립니다.** "진짜 테스트해봤어?"라고 물어보세요. 실제로 돌려서 확인하기 전까지는 완성이 아닙니다. 이건 우리가 만든 AI 기능에도 똑같이 적용됩니다 — 그래서 AI 추정 결과는 사람이 확인하도록 만들었습니다.
8. **갈아엎는 걸 무서워하지 마세요.** 1차 버전을 통째로 바꿨지만, 그 과정에서 배운 게 전부 2차 버전에 들어갔습니다. 코드는 git에 남아 있어서 언제든 되돌릴 수 있습니다.
